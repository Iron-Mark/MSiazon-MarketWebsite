# Example environment configuration for Macaroon Market
# Copy this file to .env and adjust values.

# --- Application ---
NODE_ENV=production
PORT=8080
# Comma separated list of extra CORS origins (e.g. https://yourdomain.com,http://54.169.187.175)
CORS_ORIGINS=

# --- Database (MySQL / RDS) ---
DB_HOST=your-db-hostname
DB_PORT=3306
DB_USER=your-db-user
DB_PASS=your-db-password
DB_NAME=msiazon-web

# --- AWS S3 (optional; skip to disable uploads) ---
S3_BUCKET=your-s3-bucket
S3_REGION=ap-southeast-1
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key

# If using EC2 instance profile / IAM role, you can omit S3_ACCESS_KEY & S3_SECRET_KEY
# and update S3Service to rely on default credential provider chain.
